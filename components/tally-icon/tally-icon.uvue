<template><image :src="'/static/tally/' + name + '-' + tone + '.png'" :style="{ width: size + 'px', height: size + 'px' }" mode="aspectFit" /></template>
<script setup lang="uts">
defineProps({ name: { type: String, default: 'record' }, tone: { type: String, default: 'ink' }, size: { type: Number, default: 22 } })
</script>
