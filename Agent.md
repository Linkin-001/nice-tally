# 小金账开发规范

## 项目边界

- 技术栈：uni-app x、Vue 3、UTS；当前启用 Vapor（见 `manifest.json`）。主页面为 `pages/xiaojin/index.uvue`，`pages/tally/index.uvue` 是需保持可用的旧版入口。
- 优先支持 HarmonyOS、Android、iOS 与 Web。任何平台专属能力必须通过条件编译隔离，不能影响其他端构建。
- 这是本地优先的加密记账应用。不得把账本、PIN、备份明文、密钥或用户数据写入日志、网络请求、测试快照或分析服务。

## 先做什么

1. 改动前阅读相邻代码和 `readme.md`；保留已有未提交改动，避免格式化整份文件。
2. 业务与数据校验放在 `common/xiaojin/model.ts`；加密存储放在 `common/xiaojin/vault.ts`；页面只负责展示和编排。
3. 变更数据结构、加密格式或存储 key 时，必须兼容已有本地数据和备份，提供迁移路径，并补充回归测试。
4. 完成后至少执行 `npm test`；涉及页面交互时，尽可能执行 `npm run test:ui` 或进行对应平台真机验证。HBuilderX 编译成功不等于 HarmonyOS、Android、iOS 真机验收。

## uni-app x / Vapor 样式规则

- 原生公共样式集中在 `common/xiaojin/xiaojin.css`；原生端只使用单个 class 选择器，例如 `.primary-btn`。不要使用标签、属性、后代、相邻、伪类或组合选择器，避免 Vapor CSS 编译和平台兼容问题。
- Web 专用能力（如 `100dvh`、`@media`、`box-sizing`、浏览器字体栈）必须放在 `/* #ifdef WEB */` 与 `/* #endif */` 之间；原生端不支持的 CSS 不得泄漏到公共样式。
- 使用 uni-app x 已支持的 CSS 属性和长度单位。避免依赖浏览器默认样式、复杂 `calc()`、未验证的百分比高度、CSS 变量、`position: sticky`、伪元素、属性选择器和 Web 专属动画行为。
- 不通过全局 `button` 规则修改组件；每种按钮使用独立 class，且 class 名能说明视觉语义。
- Vapor 的 button 自定义高度时：`height` 与 `line-height` 取相同像素值，显式设置 `padding-top: 0`、`padding-bottom: 0` 和 `text-align: center`。不要用 `align-items`、`justify-content` 或 `display:flex` 来对齐 button 内置文本。
- 文本行高用足够容纳字体的固定值；单行内容避免依赖默认行高。文本可能变长时应允许换行或使用明确的截断策略，不能靠固定高度裁剪。
- 使用 `background-color`、`border-color` 等完整属性而不是依赖默认 `type` 风格；动态主题颜色通过组件的 `:style` 传入。

## 平台适配矩阵

| 平台 | 必须遵守的适配点 | 真机重点 |
| --- | --- | --- |
| HarmonyOS | 使用 Vapor 兼容的单 class CSS；CPU 密集任务必须分片或后台执行，避免 `THREAD_BLOCK_3S/6S`。 | PIN 解锁、文件选择/读写、软键盘、短屏、后台切回、页面冻结日志。 |
| Android | 原生安全窗口由 `xj-security` 控制；避免直接使用 Android 类或 Activity，除非置于 `#ifdef APP-ANDROID`。 | 最近任务遮挡、返回键、文件选择权限与生命周期、不同密度/刘海/手势导航、低端机性能。 |
| iOS | 不假设文件路径、权限弹窗顺序、返回手势或安全窗口实现与 Android 相同；原生代码只置于 `#ifdef APP-IOS`。 | 安全区、底部 Home 指示器、键盘顶起、侧滑返回、文件导入/导出、深浅系统字体下的文本裁剪。 |
| Web | `window`、`document`、Blob、FileReader、URL 等 API 仅可放在 `#ifdef WEB`。 | 视口高度变化、浏览器缩放、键盘、文件下载、刷新后的本地数据与窄屏布局。 |

- 平台判断只使用 uni-app 条件编译（如 `APP-ANDROID`、`APP-IOS`、`APP-HARMONY`、`WEB`）或运行时 uni API；禁止以 User-Agent 或屏幕尺寸推断平台。
- 新增能力默认提供四端等价体验；确实没有等价 API 时，须在对应端隐藏入口或展示明确解释，不能点击后静默失败。
- 每次改动原生 API、文件、键盘、安全区、返回行为、性能或样式兼容时，至少覆盖受影响平台的构建；发布前分别完成 Android、iOS、HarmonyOS 真机验收。

## 响应式布局

- 先保证窄屏手机（320px 宽起）可用，再增强平板和桌面。页面的主容器须 `flex: 1`，可滚动区域使用 `scroll-view` 并设置 `flex: 1`、`min-height: 0`，避免底部栏挤压或内容不可滚动。
- 不假设固定设备高度。欢迎页、键盘、表单和底部弹层必须在短屏上仍可触达；仅将纯 Web 的短屏媒体查询置于 WEB 条件编译块。
- 横向行布局中，可能变长的内容使用 `flex: 1` 与 `min-width: 0`；固定尺寸的图标、金额、箭头和操作区保留宽度，避免文字将它们推出屏幕。
- 窄屏上避免两列固定宽度；网格项使用百分比宽度并验证 320px、360px、390px、430px。宽屏导航只在 `uni.getWindowInfo().windowWidth >= 900` 时使用侧栏，其他宽度保持单栏。
- 所有页面要考虑状态栏与底部安全区，读取窗口信息并通过现有 `topInset` / `bottomInset` 布局；不要硬编码刘海或手势区尺寸。
- 弹层、toast、底部栏与键盘共存时，必须验证不会被安全区或软键盘遮挡。长表单优先放在可滚动容器内。

## 跨平台交互规则

- 使用 `@click` / `@tap`、`uni.navigateTo`、`uni.chooseFile`、`uni.getFileSystemManager` 等 uni API；禁止直接在公共逻辑中调用 `window`、`document`、`localStorage` 或浏览器 File API。
- 浏览器专属实现必须在 `#ifdef WEB` 内；原生实现放在 `#ifndef WEB` 或明确平台块中。每个分支都要提供失败反馈与取消处理。
- 每个可返回的二级/三级页面都必须能通过显式返回控件和系统返回离开；Android、HarmonyOS、iOS 还应支持从屏幕左右边缘向内滑动返回上一层。手势与系统返回的结果必须一致，均走统一的 `back()` / 路由回退逻辑，不能绕过草稿确认、锁定或状态清理。
- 边缘返回手势只从屏幕边缘开始识别，且滑动距离和速度需有明确阈值；不要把普通横向滑动、图表拖动、轮播、金额输入或滚动容器的手势误判为返回。
- 横向列表操作（如归档、删除）应与边缘返回保留足够起始区域区分；有横向操作时优先提供可发现的按钮、长按菜单或二次确认，不能让用户因一次误滑丢失数据。
- iOS 的侧滑返回、Android/HarmonyOS 的系统手势返回，以及页面顶部返回按钮必须等价；当系统/运行时未提供可用边缘手势时，顶部返回按钮始终可用，并且不可被安全区、键盘或弹层遮挡。
- 不把 hover 当作唯一交互反馈。触控端的点击目标建议至少 44px；图标按钮须有可理解的文字、标签或等效可访问语义。
- 异步操作期间设置 `busy` 并防止重复提交；页面切换、锁定或卸载后，用现有 `generation` 机制丢弃过期回调。
- 加密、导入、恢复等 CPU 密集工作不得连续阻塞 ArkUI 主线程超过一帧。必须分片让出事件循环或迁移到后台任务；HarmonyOS 出现 `THREAD_BLOCK_3S/6S` 时，优先检查主线程栈。
- 删除、覆盖恢复、改密码和离开未保存编辑必须二次确认，并给出可理解的后果说明；失败消息应可行动、不能泄露敏感数据。

## 视觉与内容

- 品牌气质：安静、可信、克制、清晰。默认浅灰背景、白色表面、珊瑚强调色；主题色只用于强调、选中、主要操作和少量状态提示。
- 统一使用现有间距、圆角、字体大小和颜色令牌式约定。新增页面优先复用 `.surface`、`.primary-btn`、`.outline-btn` 等已验证类，而不是复制近似样式。
- 重要任务按视觉优先级排列：当前状态/金额、下一步动作、辅助信息。不要把每个功能都做成相同权重的卡片。
- 文案使用简洁中文，描述实际结果与风险；金额、日期、空状态、加载、错误、禁用和无权限状态都必须有明确呈现。
- UI 不得呈现管理后台、企业中台、BI 仪表盘或 SaaS 控制台效果：禁止密集 KPI 宫格、过多同权卡片、表格化设置页、大面积描边分区、夸张渐变、强阴影、图标墙、冗长面包屑和“数据驾驶舱”式首屏。
- 页面应像一款私人的移动记账工具：一次只突出一个当前任务，使用留白、清晰排版、柔和分组和渐进披露。统计页可视化服务于具体账本问题，不能为了“看起来专业”堆叠图表。
- 信息层级优先通过字号、字重、间距和少量色彩建立，而不是用大量卡片、边框、标签、悬浮按钮或复杂装饰。任何新动效都必须克制、短暂、可打断，且不妨碍读数与录入。

## 验收清单

- [ ] 320/360/390/430px 手机宽度、短屏、900px 宽屏下没有重叠、裁剪、横向溢出或不可达操作。
- [ ] 主按钮、描边按钮、浅色按钮、取消按钮文字均水平和垂直居中。
- [ ] Web、HarmonyOS、Android、iOS 专属代码均处于正确条件编译块；没有把 Web API 编进原生端，也没有把 Android/iOS/HarmonyOS API 编进其他原生端。
- [ ] 键盘弹出、返回、后台锁定、取消文件选择、导入失败和网络不可用（若涉及）均有正常回退。
- [ ] Android、HarmonyOS、iOS 的系统返回、边缘左滑/右滑返回和顶部返回按钮结果一致；手势不会误触发列表横滑或滚动。
- [ ] 数据、备份和密钥没有以明文写入日志或外部服务。
- [ ] `npm test` 通过；页面或平台改动有相应的 UI/Android/iOS/HarmonyOS 真机验证记录。
